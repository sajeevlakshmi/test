<section>
   <div class="row ">
                <div class="col-md-2" style="width: 200px;"></div>
                <div class=" col-md-10 " style="padding-left: 105px;">
                <h2 class="text-center">Notes</h2>
                <form action="/teacher_notes" method="post" enctype="multipart/form-data">

                    <div class="form-group">
                        <label for="name">Name</label>
                        <input type="text" class="form-control" name="name" value="" required>
                        <label for="">Document File</label>

                        <input type="file" class="form-control" name="document" onchange="" accept=".pdf" required>

                        <label for="">Video</label>

                        <input type="file" class="form-control" name="video" onchange="" accept=".mp4" required>


                        <button type="submit" class="btn btn-success mt-4">Save</button>
                    </div>
                </form>
                <div>

                    <table class="table mt-5">
                        <thead>
                            <tr>
                                <th scope="col">Topic </th>

                                <th scope="col">video</th>
                                <th scope="col">File</th>
                                <th scope="col">Action</th>

                            </tr>
                        </thead>
                        <tbody>
                            {{#each notes}}
                            <tr>

                                <td>{{this.name}}</td>

                                <td>
                                    <video controls loop width="100px" height="100px">
                                        <source src="/video/{{this._id}}{{this.filename}}.mp4">
                                    </video>
                                <td><a
                                        href="/teacher/notes/{{this._id}}/{{this.documentname}}">{{this.documentname}}</a>
                                </td>

                                <td>
                                    <a href="/teacher/delete-notes/{{this._id}}" class="btn btn-danger"
                                        onClick=" return confirm('Are you sure to delete{{this.name}}')">Delete</a>
                                </td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>


            </div>
        </div>
    
</section>